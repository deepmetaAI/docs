import { Libraries } from '@/components/Libraries'

export const metadata = {
  title: 'Protocol SDK',
  description:
    'Protocol 提供精心调优的 JavaScript、Ruby、PHP、Python 和 Go 库，让您的开发更轻松，在使用 API 时获得最佳体验。',
}

export const sections = [
  { title: '官方库', id: 'official-libraries' },
]

# Protocol SDK

与 Protocol API 交互的推荐方式是使用我们的官方 SDK。目前，Protocol 提供精心调优的 JavaScript、Ruby、PHP、Python 和 Go 库，让您的开发更轻松，在使用 API 时获得最佳体验。

<Libraries />
